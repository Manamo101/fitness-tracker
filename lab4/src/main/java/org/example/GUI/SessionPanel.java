package org.example.GUI;

import javax.swing.*;

public class SessionPanel extends JPanel {
    JLabel label1;
    SessionPanel(){
        label1 = new JLabel("Sessions");
        this.add(label1);
    }
}
